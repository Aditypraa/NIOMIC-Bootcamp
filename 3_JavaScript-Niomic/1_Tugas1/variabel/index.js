// VAR
var msg1 = "var : var bisa di rubah di mana saja";
console.log(msg1);

//LET
if (true) {
  let msg2 = "let : let hanya bisa di rubah di satu tempat/scope";
  console.log(msg2);
}

//CONST
const msg3 = "Const : Nilai Yang sudah tetap/constan dan tidak bisa dirubah";

// Tnpa var,let dan const
msg4; // Tanpa let,const, dan var : ini harus ada valuenya jika tidak akan is not undifined
