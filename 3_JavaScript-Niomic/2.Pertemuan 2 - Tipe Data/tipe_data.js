// Tipe Data Number
var number = 20;
var number2 = 20.1;
var number3 = -20;
console.log(number);
console.log(number2);
console.log(number3);

// Tipe Data String
var string = "Tipe Data String";
console.log(string);

// Tipe Data Boolean
var benar = true;
var salah = false;

let cekHasil = salah == true ? true : false;

// Tipe Data Null dan Undifined
var tipeNull = null; // sudah diketahui tapi nilainya memang kosong
console.log(tipeNull);

var tipeUndefined; // Nilai atau Valuenya belum di ketahui
console.log(tipeUndefined);

// Tipe Data Array
var array = [1, 2, 3, 4, 5, 6, 7];
console.log(array);
var array2 = ["samsul", "yanto", "yadi", "julian"];
console.log(array2);
