// import os from "os";

// console.info(os.platform());
// console.table(os.cpus());

function sapa(nama) {
  return `sapa, ${nama}`;
}
function hallo(nama) {
  return `Hallo ${nama}`;
}

export { sapa, hallo };
